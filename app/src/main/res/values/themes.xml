<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌐 Tema base de la aplicación -->
    <style name="Base.Theme.OMG" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fondo que se verá antes de cualquier actividad -->
        <item name="android:windowBackground">@android:color/transparent</item>

        <!-- 🔥 Desactiva splash automático en Android 12+ -->
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <!-- ✅ Fuente por defecto para toda la app -->
        <item name="android:fontFamily">@font/din_round_pro</item>
    </style>

    <!-- Tema principal de la app -->
    <style name="Theme.OMG" parent="Base.Theme.OMG" />

    <!-- 🚀 Tema exclusivo para SplashActivity -->
    <style name="Theme.SplashInstant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fondo igual al splash para evitar parpadeo -->
        <item name="android:windowBackground">#00BCD4</item>

        <!-- Opcional: si tu layout ya tiene fondo -->
        <!-- <item name="android:windowBackground">@android:color/transparent</item> -->

        <!-- Elimina decoraciones del sistema -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

        <!-- 🔥 Desactiva splash automático en Android 12+ -->
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>

</resources>
